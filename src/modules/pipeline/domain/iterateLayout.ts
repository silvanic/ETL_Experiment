export const ITERATE_LAYOUT = {
  minWidth: 520,
  minHeight: 240,
  paddingRight: 48,
  paddingBottom: 48,
} as const

export const ITERATE_CHILD_ESTIMATE = {
  defaultWidth: 180,
  defaultHeight: 64,
  smallNodeWidth: 140,
} as const
